function Configuration() {
 
  /*Make table -> fetch full db-> identify system -> 1 input at a time -> identify next part input -> find corresponding parts per description 
  -> find corresponding parts per used -> populate two menus to separate the two recomendations -> once next part is inputed remove tables and move to the next "next" part 
*/
  return (
    <div>
    
    </div>
  );
}

export default Configuration;
